<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Base application theme -->
    <style name="Base.Theme.Hotpot0" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Set the background color for system bars -->
        <item name="android:statusBarColor">@color/statusbar</item>
        <item name="android:navigationBarColor">@color/navbar</item>

        <!-- Keep icons visible on light background -->
        <item name="android:windowLightStatusBar">true</item>
    </style>

    <!-- Your app’s active theme -->
    <style name="Theme.Hotpot0" parent="Base.Theme.Hotpot0" />

    <style name="Theme.MyApp" parent="Theme.Material3.DayNight.NoActionBar" />

</resources>